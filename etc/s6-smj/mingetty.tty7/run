#!/bin/execlineb -P
#we log into X from vt7 wait for mdevd-coldplug?
foreground { ../1shots/data/waitup hostname }
foreground { s6-svwait -U /run/service/mdevd }
# we normally log in on vt7 for X11
foreground { chvt 7 }
/sbin/mingetty tty7 linux
